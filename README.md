# 🏋️ Fitness Club Management System — Система учёта клиентов, тренировок и посещаемости

Полнофункциональное веб-приложение для автоматизации работы фитнес-клуба:
управление клиентами, тренерами, расписанием тренировок, бронированиями и учётом посещаемости.

---

## 📁 Структура проекта

```
fitness-club-system/
├── src/
│   └── main/
│       ├── java/com/example/demo/
│       │   ├── controller/       # MVC-контроллеры (Client, Trainer, Admin)
│       │   ├── config/           # Spring Security
│       │   ├── entity/           # JPA-сущности (User, Client, Trainer, Session, Booking)
│       │   ├── repository/       # Интерфейсы для работы с БД
│       │   └── service/          # Бизнес-логика
│       └── resources/
│           ├── templates/        # Thymeleaf HTML-шаблоны
│           ├── static/           # CSS / JS
│           └── application.properties
├── pom.xml                       # Maven зависимости
└── README.md
```

---

## 🛠 Технологический стек

### 🖥 Backend

| Технология            | Описание                            |
| --------------------- | ----------------------------------- |
| **Java 21**           | основной язык разработки            |
| **Spring Boot 3.2**   | backend-фреймворк                   |
| **Spring MVC**        | обработка HTTP-запросов             |
| **Spring Security 6** | авторизация и разграничение доступа |
| **JPA + Hibernate**   | ORM для работы с БД                 |
| **MySQL**             | основная база данных                |
| **Maven**             | сборка проекта                      |

---

### 🎨 Frontend

| Технология                     | Описание                              |
| ------------------------------ | ------------------------------------- |
| **Thymeleaf**                  | динамические HTML-страницы            |
| **Bootstrap 5**                | адаптивный UI                         |
| **Thymeleaf Security Dialect** | показ элементов в зависимости от роли |

---

## ✨ Основные возможности системы

### 🔐 Авторизация и роли

* Роли: **ADMIN**, **TRAINER**, **CLIENT**
* Страница логина
* Разграничение доступа по ролям
* Личный кабинет каждого пользователя

---

### 🧑‍💼 Функционал администратора

Админ может:

✔ создавать и редактировать клиентов
✔ добавлять тренеров
✔ управлять расписанием всех тренеров
✔ просматривать список записанных клиентов
✔ отмечать **посещаемость** (пришёл / пропустил)
✔ просматривать историю бронирований

---

### 🧑‍🏫 Функционал тренера

Тренер:

✔ создаёт собственное расписание
✔ редактирует свои тренировки
✔ видит всех записанных клиентов
✔ может контролировать количество участников

---

### 🧍‍♂️ Функционал клиента

Клиент:

✔ видит список доступных тренировок
✔ может **записаться** на занятие
✔ видит свои бронирования
✔ не может записаться, если достигнут лимит мест

---

### 🗂 Управление расписанием

Каждая тренировка содержит:

* название занятия
* тренера
* дату и время
* длительность
* лимит участников
* текущее количество записанных клиентов

Запись автоматически увеличивает `currentParticipants`.

Если лимит достигнут — запись блокируется.

---

### 📋 Учет посещаемости

Для каждого клиента на тренировку фиксируется статус:

* **NONE** — не отмечено
* **ATTENDED** — посетил
* **MISSED** — пропустил

Используется enum:

```java
public enum AttendanceStatus {
    NONE,
    ATTENDED,
    MISSED
}
```

---

## 🚀 Запуск проекта

### 1. Клонирование репозитория

```bash
git clone https://github.com/USERNAME/fitness-club-system.git
cd fitness-club-system
```

---

### 2. Настройка MySQL

Создать базу данных:

```sql
CREATE DATABASE fitness_accounting_db;
```

Убедиться, что в `application.properties` указаны ваши настройки:

```properties
spring.datasource.url=jdbc:mysql://localhost:3306/fitness_accounting_db
spring.datasource.username=root
spring.datasource.password=1234
spring.jpa.hibernate.ddl-auto=update
```

---

### 3. Запуск

```bash
mvn spring-boot:run
```

Перейти в браузер:

```
http://localhost:8080/
```

---

## 🌐 Доступ к основным страницам

| Роль                   | URL                  |
| ---------------------- | -------------------- |
| **Логин**              | `/login`             |
| **Кабинет админа**     | `/admin/dashboard`   |
| **Кабинет тренера**    | `/trainer/dashboard` |
| **Кабинет клиента**    | `/client/dashboard`  |
| **Расписание клиента** | `/client/schedule`   |
| **Расписание тренера** | `/trainer/schedule`  |

---

## 🔑 Тестовые пользователи

| Логин   | Пароль  | Роль    |
| ------- | ------- | ------- |
| admin   | admin   | ADMIN   |
| trainer | trainer | TRAINER |
| client  | client  | CLIENT  |

---

## 📦 Основные контроллеры

| Контроллер          | Endpoint                | Назначение                                     |
| ------------------- | ----------------------- | ---------------------------------------------- |
| `AdminController`   | `/admin/*`              | управление клиентами, тренерами, посещаемостью |
| `TrainerController` | `/trainer/*`            | создание расписания                            |
| `ClientController`  | `/client/*`             | запись на занятия                              |
| `AuthController`    | `/login`                | авторизация                                    |
| `BookingController` | `/client/schedule/book` | бронирование                                   |

---

## 🧱 Пример логики бронирования

```java
public boolean bookSession(Long sessionId, Long clientId) {
    TrainingSession session = sessionRepo.findById(sessionId).orElseThrow();

    if (session.getCurrentParticipants() >= session.getMaxParticipants()) {
        return false; // нет мест
    }

    Booking booking = new Booking();
    booking.setSession(session);
    booking.setClient(clientRepo.findById(clientId).orElseThrow());
    session.setCurrentParticipants(session.getCurrentParticipants() + 1);

    bookingRepo.save(booking);
    sessionRepo.save(session);

    return true;
}
```

---

## 📌 Пример учёта посещаемости

```java
public void markAttended(Long bookingId) {
    Booking booking = repo.findById(bookingId).orElseThrow();
    booking.setStatus(AttendanceStatus.ATTENDED);
    repo.save(booking);
}
```

---

## 👤 Автор

Закирьянова Камила Бауржановна
Курсовая работа по дисциплине
**Объектно-ориентированное программирование Java**
